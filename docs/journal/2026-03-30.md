# 2026-03-30 — Sonntag

## Aufgaben
- [x] WLAN-Seite gebaut: PPSK-Passwörter + QR-Codes aus UniFi API
- [x] Alle 14 RW-Clients Netzwerke mit PPSK-Passwörtern ausgestattet
- [x] ISP-Übersichtsseite mit Init7-Info, WLAN, TV7, Verbindungsnachweis
- [x] FPÜV-Verbindungsprotokollierung: API pollt UniFi alle 5 Min → connection_log DB
- [x] Syslog Collector Docker Service (UDP+TCP, JSON+Syslog Parser)
- [x] UDM Remote Syslog auf 100.64.2.24:5514 konfiguriert (via API)
- [x] Verbindungsnachweis-Seite mit Suche + CSV-Export (Admin-only)
- [x] TV7 Web-Client: Kanalliste, Player mit mpegts.js, Gruppen-Filter
- [x] udpxy Multicast-zu-HTTP Proxy (erst Docker, dann LXC wegen Multicast)
- [ ] CT 109 (tv-proxy) fertig einrichten: udpxy + systemd Service
- [ ] tv.rosenweg4303.ch DNS-Eintrag (intern, ohne CF Tunnel)
- [ ] TV7 Stream testen

## Notizen
- Docker Overlay-Netzwerk unterstützt kein UDP Multicast → udpxy muss in eigenem LXC laufen
- UniFi SIEM-Integration sendet noch keine Events — rsyslogd über API aktiviert
- WLAN-Seite: apiFetch() verursacht auto-logout bei 401, direkter fetch() nötig
- QR-Code CDN (jsdelivr) lieferte kein qrcode.js → QRious lokal eingebunden
- TV7 Playlist nur UDP Multicast, kein HTTP/HLS → udpxy als Proxy zwingend

## Offene Punkte
- Syslog/SIEM Events von UDM kommen noch nicht zuverlässig
- Gast-WLAN FPÜV-Logging (Captive Portal?)
- TV7 funktioniert erst wenn udpxy auf CT 109 Multicast empfangen kann
