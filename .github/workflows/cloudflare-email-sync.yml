name: Sync Cloudflare Email Routing

on:
  push:
    branches:
      - main
    paths:
      - 'ausschuss-kontakte.json'
  workflow_dispatch:  # Erlaubt manuelles Triggern

jobs:
  sync-email-routing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          pip install requests

      - name: Sync Email Routing to Cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ZONE_ID: ${{ secrets.CLOUDFLARE_ZONE_ID }}
        run: |
          python scripts/sync-cloudflare-email.py

      - name: Summary
        run: |
          echo "✅ Cloudflare E-Mail-Routing wurde erfolgreich synchronisiert!"
          echo "📧 Geprüfte Datei: ausschuss-kontakte.json"
